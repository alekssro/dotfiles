# dotfiles

# Install zsh

```
sudo apt install zsh
```

# Set zsh as default shell

```
chsh -s $(which zsh)
```

At this point, restart session to apply changes.

# Install dotfiles

```
./init.zsh
```

